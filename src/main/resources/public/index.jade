doctype html
html
  head
    title= pageName
  body
    h1 #{pageName} | Yea
    ol#books
      for book in books
        - var x = 5;
        - console.log(x);
        if book.available
          li #{book.name} for #{book.price} €